<!-- GENERATED by src/gen-docs.mjs - do not edit by hand. Edit the registry and re-run `npm run docs`. -->

# The 15 capabilities

A capability is a **job to be done** inside a build, not a product. Typed classification is one of these. Keeping this layer provider-agnostic is what stops the catalogue dying when a vendor changes.

Across the 1442 projects, the median build draws on about 3 of them.

| Capability | Projects | Latency | Cost shape | Job |
|---|---:|---|---|---|
| [Typed classification & decision](#classify) | 1442 | ~100ms | per item, ~free | Turn messy input into a typed answer software can branch on, with a confidence number. |
| [Orchestration & scheduling](#orchestrate) | 748 | n/a | compute | Run the whole thing repeatedly, in parallel, without tripping a rate limit. |
| [Data store](#store) | 744 | ms | per GB | Hold the corpus, the judgments, and the provenance. |
| [Fetch, scrape & ingest](#fetch) | 703 | varies | bandwidth | Get the raw corpus in: pages, feeds, APIs, catalogues, PDFs. |
| [Realtime transport](#realtime) | 373 | ms | connection time | Stream partial results so the interface never looks frozen. |
| [Safety & guardrails](#guardrail) | 309 | ~0 | one extra question | Refuse to do the irreversible thing without a human. |
| [Browser automation](#browser) | 200 | 100ms-2s per action | compute | Drive a real web page: click, type, select, scroll, read state. |
| [Search & retrieval](#search) | 195 | 50-500ms | per query | Find the candidate set worth judging. |
| [Text generation](#textgen) | 132 | 200ms-2s | per token | Write the strings a typed model structurally cannot emit: queries, form values, copy, code. |
| [Image understanding](#vision) | 66 | 100ms-3s | per image | Read what is in a picture: a label, a garment, a screen, a document. |
| [Page state extraction](#domsnapshot) | 58 | <50ms | free | Turn a live page into an indexed list of things that can be acted on. |
| [Voice input](#voicein) | 33 | ~100-300ms | per minute | Turn speech into text, and know when the speaker has finished. |
| [Mobile & desktop control](#mobile) | 15 | 200ms-2s | compute | Drive a native app rather than a web page. |
| [Image generation & try-on](#imagegen) | 9 | 2-30s | per image | Render something that does not exist yet: a garment on a body, a variant, a scene. |
| [Voice output](#voiceout) | 4 | 200ms-1s | per character | Speak back, or confirm a risky action out loud. |

---

## Typed classification & decision
<a id="classify"></a>

**Turn messy input into a typed answer software can branch on, with a confidence number.**

Used by **1442** of 1442 projects, most in Engineering (836), Making & Motion (214), Communication & Reach (138). Typical latency ~100ms; billed per item, ~free.

| Provider | Kind | Notes |
|---|---|---|
| [TypeSafe Jev](https://docs.typesafe.ai) | hosted | $0.042/MTok in, output free; 70-500ms; 1,200 req/min |
| [kev](https://github.com/jaredpalmer/kev) | self-host | Qwen 0.5-8B, runs on a laptop; ~6-7pts behind out-of-distribution |
| [SemIf](https://github.com/TheoLeeCJ/SemIf) | self-host | logit readout, zero output tokens, WebGPU demo |
| [openjev-sglang](https://github.com/ekzhang/openjev-sglang) | self-host | prefill-only, needs a serious GPU |
| LLM + structured output | fallback | 10-100x slower and dearer; fine below ~100 items |

## Orchestration & scheduling
<a id="orchestrate"></a>

**Run the whole thing repeatedly, in parallel, without tripping a rate limit.**

Used by **748** of 1442 projects, most in Engineering (490), Making & Motion (66), Communication & Reach (65). Typical latency n/a; billed compute.

| Provider | Kind | Notes |
|---|---|---|
| Plain script + worker pool | code | the honest default; cap in-flight yourself |
| Cron / durable queues | hosted | scheduled and durable fan-out |
| n8n | gui | a community TypeSafe node exists, unverified |

> **Watch out.** 1,200 req/min is the real ceiling. One item per request means about 20 items a second, whatever your budget says.

## Data store
<a id="store"></a>

**Hold the corpus, the judgments, and the provenance.**

Used by **744** of 1442 projects, most in Engineering (507), Communication & Reach (57), Making & Motion (55). Typical latency ms; billed per GB.

| Provider | Kind | Notes |
|---|---|---|
| SQLite / Postgres | db | judgments are rows; keep the raw input beside the verdict |
| Static JSON | file | fine into the low thousands, trivially deployable |
| Object storage | hosted | media and large artifacts |

## Fetch, scrape & ingest
<a id="fetch"></a>

**Get the raw corpus in: pages, feeds, APIs, catalogues, PDFs.**

Used by **703** of 1442 projects, most in Engineering (458), Communication & Reach (62), Operations & Money (57). Typical latency varies; billed bandwidth.

| Provider | Kind | Notes |
|---|---|---|
| Embedded JSON blob | technique | always look here first; beats rendering a page |
| HTTP + parser (cheerio / BeautifulSoup) | library | cheapest path for server-rendered HTML |
| Headless browser | library | only when the data is client-rendered |
| Official API or bulk export | source | always preferred where it exists |

## Realtime transport
<a id="realtime"></a>

**Stream partial results so the interface never looks frozen.**

Used by **373** of 1442 projects, most in Making & Motion (158), Engineering (108), Communication & Reach (53). Typical latency ms; billed connection time.

| Provider | Kind | Notes |
|---|---|---|
| WebSocket | protocol | supported on Vercel Functions |
| SSE / streaming response | protocol | works on the Node runtime, no edge needed |

## Safety & guardrails
<a id="guardrail"></a>

**Refuse to do the irreversible thing without a human.**

Used by **309** of 1442 projects, most in Engineering (224), Communication & Reach (29), Operations & Money (17). Typical latency ~0; billed one extra question.

| Provider | Kind | Notes |
|---|---|---|
| Typed risk gate | pattern | classify read-only / reversible / irreversible before acting |
| Confidence threshold | pattern | fitted on labelled data, never guessed |

> **Watch out.** Ride the risk question along in the SAME call as the action question. It costs almost nothing and it is the difference between a demo and a product.

## Browser automation
<a id="browser"></a>

**Drive a real web page: click, type, select, scroll, read state.**

Used by **200** of 1442 projects, most in Engineering (101), Communication & Reach (49), Everyday & Commerce (18). Typical latency 100ms-2s per action; billed compute.

| Provider | Kind | Notes |
|---|---|---|
| [Playwright](https://playwright.dev) | library | headless driver, used by jev-browser |
| Chrome DevTools Protocol | protocol | jev-ultrafast connects directly |
| [Stagehand](https://github.com/browserbase/stagehand) | framework | act/observe API, has a Jev path |
| Chrome extension APIs | in-browser | the only route to a logged-in session |

## Search & retrieval
<a id="search"></a>

**Find the candidate set worth judging.**

Used by **195** of 1442 projects, most in Engineering (96), Operations & Money (36), Communication & Reach (27). Typical latency 50-500ms; billed per query.

| Provider | Kind | Notes |
|---|---|---|
| Keyword / BM25 | local | Stagehand shortlists candidates this way before calling Jev |
| Vector / embeddings | hosted | when wording differs from meaning |
| Search API | hosted | Jev Search pairs a search API with typed ranking |

> **Watch out.** Retrieve wide and cheap, then judge narrow and typed. Never ask the typed model to scan everything.

## Text generation
<a id="textgen"></a>

**Write the strings a typed model structurally cannot emit: queries, form values, copy, code.**

Used by **132** of 1442 projects, most in Engineering (66), Everyday & Commerce (17), Communication & Reach (16). Typical latency 200ms-2s; billed per token.

| Provider | Kind | Notes |
|---|---|---|
| [inception/mercury-2.5 via OpenRouter](https://openrouter.ai) | hosted | the default typing model in jev-ultrafast |
| Claude / GPT / Gemini | hosted | when quality beats latency |
| Keyword heuristics | local | the no-key fallback in jev-browser |

## Image understanding
<a id="vision"></a>

**Read what is in a picture: a label, a garment, a screen, a document.**

Used by **66** of 1442 projects, most in Making & Motion (29), Engineering (17), Operations & Money (6). Typical latency 100ms-3s; billed per image.

| Provider | Kind | Notes |
|---|---|---|
| OCR (Tesseract or on-device Vision) | local | the macOS computer-use demo uses on-device OCR |
| Vision LLM | hosted | for open-ended understanding |
| CoreML segmentation | local | segment UI or product regions before classifying |

> **Watch out.** Jev is text-only. Every image must become text or structured fields BEFORE the typed call.

## Page state extraction
<a id="domsnapshot"></a>

**Turn a live page into an indexed list of things that can be acted on.**

Used by **58** of 1442 projects, most in Engineering (34), Everyday & Commerce (12), Making & Motion (5). Typical latency <50ms; billed free.

| Provider | Kind | Notes |
|---|---|---|
| Injected snapshot script | custom | atomic capture of visible controls, as in jev-ultrafast |
| Raw DOM query | custom | what jev-browser chose; the a11y tree under-reports inputs |
| Accessibility tree | builtin | cleaner but misses elements, including a DuckDuckGo search box |

## Voice input
<a id="voicein"></a>

**Turn speech into text, and know when the speaker has finished.**

Used by **33** of 1442 projects, most in Engineering (15), Everyday & Commerce (7), Making & Motion (5). Typical latency ~100-300ms; billed per minute.

| Provider | Kind | Notes |
|---|---|---|
| Web Speech API | browser | free, zero install, cloud backed |
| [whisper.cpp local](https://github.com/ggerganov/whisper.cpp) | local | ~100ms, audio never leaves the device |
| Deepgram / realtime STT | hosted | streaming partials, paid |

> **Watch out.** Shipped Jev voice projects use a 200ms debounce plus a 600-900ms silence timer INSTEAD of a real VAD library. Cheap, and good enough.

## Mobile & desktop control
<a id="mobile"></a>

**Drive a native app rather than a web page.**

Used by **15** of 1442 projects, most in Engineering (8), Making & Motion (2), People & Care (2). Typical latency 200ms-2s; billed compute.

| Provider | Kind | Notes |
|---|---|---|
| Appium / Espresso | library | used in the Jev end-to-end testing projects |
| macOS Accessibility API | os | how the voice-controlled Mac demos dispatch |

> **Watch out.** No Jev-specific mobile integration writeup was found in this research pass. Treat mobile as an open gap.

## Image generation & try-on
<a id="imagegen"></a>

**Render something that does not exist yet: a garment on a body, a variant, a scene.**

Used by **9** of 1442 projects, most in Making & Motion (8), Communication & Reach (1). Typical latency 2-30s; billed per image.

| Provider | Kind | Notes |
|---|---|---|
| [Virtual try-on diffusion (fal / replicate)](https://fal.ai) | hosted | garment-on-person rendering |
| General image models | hosted | Seedream, Imagen, nano-banana |

> **Watch out.** The slowest thing in any stack. Never put it on the critical path - precompute it or stream it in.

## Voice output
<a id="voiceout"></a>

**Speak back, or confirm a risky action out loud.**

Used by **4** of 1442 projects, most in Engineering (1), Making & Motion (1), Everyday & Commerce (1). Typical latency 200ms-1s; billed per character.

| Provider | Kind | Notes |
|---|---|---|
| Web Speech Synthesis | browser | free |
| System TTS | os | macOS talk-back in the Jev Mac demos |
| ElevenLabs or hosted TTS | hosted | quality, paid |

> **Watch out.** Several shipped voice demos skip TTS entirely and show a toast. Faster, and usually enough.
